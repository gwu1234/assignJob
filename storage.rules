service firebase.storage {
  match /b/{bucket}/o {
    match /{filename}/{allPaths=**} {
      allow read, write: if request.auth.token.email =="info@jc.com" && filename =="jc+infojccom";
    }
  }
}
